.import mem;
.import core;

.let i32 = %Int 4294967296;
.let i8 = %Int 256;

.cn outer [mem: %mem.M, return_outer : .Cn [%mem.M]] = {
   return_outer (mem)
};

.cn .extern main [mem: %mem.M, argc: i32, argv : %mem.Ptr («⊤:.Nat; %mem.Ptr («⊤:.Nat; %Int 256», 0:.Nat)», 0:.Nat), return_main : .Cn [%mem.M, i32]] = {
    .cn exit [mem2: %mem.M] = {
        return_main (mem2, 1:i32)
    };

    outer ( mem, exit )
};