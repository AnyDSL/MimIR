
.import mem;
.import core;

.let i32 = %Int 4294967296;
.let f32 = %Real 4294967296;
.let i8 = %Int 256;

.cn loop_body [mem: %mem.M, return : .Cn [%mem.M]] = {
   return(mem)
};

.cn .extern main [mem: %mem.M, argc: i32, argv : %mem.Ptr («⊤:.Nat; %mem.Ptr («⊤:.Nat; %Int 256», 0:.Nat)», 0:.Nat), return : .Cn [%mem.M, i32]] = {
    .cn exit [mem2: %mem.M] = {
        return (mem2, 1:i32)
    };

    loop_body ( mem, exit )
};
