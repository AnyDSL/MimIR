// RUN: %mim %s
plugin tensor;
plugin refly;
plugin core;
plugin direct;
plugin affine;

let m0 = ((1, 2), (3, 4));

lam r(): Ring = (Nat, 0, %core.nat.add, %core.nat.mul);
let R = r ();

let dot2d = %tensor.prod_2d_lam R (m0, m0);