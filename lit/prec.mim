// RUN: %mim %s -o - | FileCheck %s

lam extern f (x: Nat): Nat = x;
lam extern higher(f: (Nat → Nat) → Nat, x: Nat): Nat = f (λ y: Nat = y);

// CHECK-DAG: lam extern f (x_{{[0-9]+}}: Nat): Nat =
// CHECK-DAG: f_{{[0-9]+}}: (Nat → Nat) → Nat
