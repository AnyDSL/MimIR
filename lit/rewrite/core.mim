// RUN: %mim %s

plugin core;

 rule (x:Nat) : %core.nat.add (x, 0) => x;

rule (x:Nat) : %core.nat.sub (0,x) => 0;
rule (x:Nat) : %core.nat.mul (0, x) => 0;

 rule (x:Nat) : %core.nat.mul (1, x) => x;

rule (x: Nat) : %core.nat.sub (x, 0) => x;
