// RUN: %mim %s -o -

axm %foo.Phase: *;

axm %foo.phase1: Bool        → %foo.Phase;
axm %foo.phase2: []          → %foo.Phase;
axm %foo.phase3: [Nat, Bool] → %foo.Phase;

rec PiPeline: * = %foo.Phase → PiPeline; // got the pun? XD

axm %foo.pipe: PiPeline;

let pipeline = %foo.pipe
    (%foo.phase1 tt)
    (%foo.phase2 ())
    (%foo.phase3 (23, tt));
