plugin core;
plugin eqsat;

fun extern _rulesets(): %eqsat.Ruleset =
    return (%eqsat.rulesets (
        %eqsat.core,
        %eqsat.math,
    ));

fun extern main(mem: %mem.M 0, argc: I32): [%mem.M 0, I32] =
   return (mem, 0I32)
