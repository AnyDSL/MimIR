// RUN: rm -f %t.ll
// RUN: (! %mim %s 2>&1) | FileCheck %s

plugin core;
plugin refly;

let n = %core.nat.add (3, 4);
let _ = %refly.assert (n, 7);
let _ = %refly.assert (n, 8);

// CHECK: '7' and '8' not alpha-equivalent
