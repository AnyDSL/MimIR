// UNSUPPORTED: always
// RUN: %mim %s -o -

import compile;

// we want to check the normalizer working → that's much easier not having the lower_regex phase running :)
lam extern _compile(): %compile.Phase =
    %compile.phase.man ff (
        %compile.internal_cleanup_phase,
        %compile.pass2phase %compile.ret_wrap_pass,
    );
