// UNSUPPORTED: always
// RUN: %mim %s -o -

import phase;

// we want to check the normalizer working → that's much easier not having the lower_regex phase running :)
lam extern _compile(): %phase.Phase =
    %phase.phases ff (
        %phase.internal_cleanup_phase,
        %phase.passes %phase.ret_wrap_pass,
    );
