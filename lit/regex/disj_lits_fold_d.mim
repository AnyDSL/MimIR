// RUN: %mim %s -o - -P %S | FileCheck %s

import mem;
plugin regex;

import dont_compile.mim;

let Top = ⊤:Nat;

con extern disj(mem: %mem.M 0, string: %mem.Ptr (<<Top; I8>>, 0), return: Cn [%mem.M 0, Bool, I32]) =
    let pattern = %regex.disj (%regex.cls.d, %regex.lit '0', %regex.lit '5', %regex.lit '9');
    return (pattern (mem, string, 0I32));
// CHECK: %regex.range (48I8, 57I8)
// CHECK-NOT: %regex.lit
